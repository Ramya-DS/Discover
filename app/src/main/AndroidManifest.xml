<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.discover">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DiscoverApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".loginScreen.LoginActivity"></activity>
        <activity android:name=".genreScreen.GenreMediaActivity" />
        <activity android:name=".showsScreen.SeasonActivity" />
        <activity android:name=".showsScreen.ShowActivity" />
        <activity android:name=".mediaScreenUtils.MediaReviewActivity" />
        <activity android:name=".category.MediaCategoryActivity" />
        <activity
            android:name=".searchScreen.SearchActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".filterScreen.ui.FilterActivity" />
        <activity android:name=".movieScreen.MovieActivity" />
        <activity
            android:name=".firstScreen.MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mediaDisplay.MediaListActivity" />
    </application>

</manifest>